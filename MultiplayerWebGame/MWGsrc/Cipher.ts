module MWG {
    export class Cipher {
        public static encodeButton(buttonCode: ButtonCode): string {
            return "";
        }
        public static decodeButton(buttonData: string): ButtonCode {
            return ButtonCode.A;
        }
    }
}